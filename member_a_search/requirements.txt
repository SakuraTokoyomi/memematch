# 核心依赖（指定兼容版本）
numpy>=1.24.0,<2.0
pandas>=2.0.0
Pillow>=9.0.0

# 向量检索
faiss-cpu>=1.7.4

# 向量模型（会自动安装 torch, transformers）
sentence-transformers>=2.2.0

# 可选：加速下载（如果使用清华镜像）
# --index-url https://pypi.tuna.tsinghua.edu.cn/simple