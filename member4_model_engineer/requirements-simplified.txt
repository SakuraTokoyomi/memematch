# 简化版依赖（适用于Python 3.14或无法安装PyTorch的情况）
# 注意：由于Python 3.14太新，PyTorch暂不支持
# 建议：使用 Python 3.9-3.11 版本

# 如果要继续使用Python 3.14，可以先安装这些：
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
pyyaml>=6.0
tqdm>=4.65.0

# 然后手动尝试安装（可能失败）：
# pip install sentence-transformers --no-deps
# pip install transformers --no-deps

# 推荐方案：使用较低版本Python
# 1. 安装 Python 3.11: brew install python@3.11
# 2. 重新创建虚拟环境: python3.11 -m venv venv
# 3. 安装依赖: pip install -r requirements.txt


