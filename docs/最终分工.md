# 🚀 **Meme Agent 项目 · 六人分工（最终版）**

## **👑 0. 负责人（你/PM）**

- 负责项目总体设计、PRD、架构规划、进度协调
- 合并成员产出，写最终报告 + 演示
- 审核所有 Pull Requests
- 负责 Agent 主逻辑整合
- 负责最终 Demo 演示

**可交付物**

- 完整 PRD（你已经有了）
- 项目架构图
- README
- Demo 视频
- 集成版 Meme Agent

---

# 👤 **成员 A：检索模块负责人（Search & Embedding Engineer）**

### **职责**

1. 构建 memematch 的 embedding pipeline
2. 使用 CLIP / SigLIP 计算所有 meme 的 embedding
3. 建立 & 保存 Faiss 向量数据库
4. 实现 search_meme(query, top_k) 工具函数
5. 优化检索速度、排序、召回率
6. 提供 API 给 Agent

### **可交付物**

- `/search/embedding_builder.py`
- `/search/faiss_index.py`
- `search_meme()` 完整工具函数
- 单元测试（检索准确率）

**你的小组完成后，这块就能复用原 memematch，也可以进一步优化。**

---

# 👤 **成员 B：LLM Agent 策略负责人（Agent Design & LLM Engineer）**

### **职责**

1. 实现 LLM Agent 的主推理循环（ReAct / Function Calling）
2. 设计工具调用逻辑：search / generate / refine
3. 设计“情绪分类”、“查询改写” prompt
4. 实现工具函数调用调度层
5. 调参与安全性处理（错误重试、fallback）

### **可交付物**

- `/agent/agent_core.py`
- `refine_query()`
- `classify_sentiment()`
- tool-calling 管理器
- Agent 多轮推理逻辑（完整工作闭环）

**这是整个项目的核心角色之一。**

---

# 👤 **成员 C：Meme 生成模块负责人（Generation Engineer）**

### **职责**

负责 *generate_meme()* 所有功能，包括：

### **A. 模板生成（必须）**

- PIL template（Drake、Doge、Wojak）
- 文本渲染、排版、自适应字号
- 自动保存生成文件

### **B. 可选：文生图生成（进阶，需要 GPU）**

- 接入 FLUX / SDXL / OpenAI Image API
- 实现：搜索不到时 fallback 调用生成模型

### **可交付物**

- `/generate/template_generator.py`
- `/generate/aigc_generator.py`（如果用 API）
- `generate_meme(text)` 工具
- 单元测试：生成速度 < 0.5s（模板版）

**不需要大量 GPU，模板版即可。**

---

# 👤 **成员 D：前端 & UI 负责人（Frontend Engineer）**

### **职责**

使用 vue 构建前端 UI：

- 输入文本框
- 展示返回的 meme
- 展示所有 top-k 候选
- 支持下载 meme
- 展示 Agent 工具调用流程（debug 面板）
- 支持上传图片（可选）

### **可交付物**

- UI 布局设计图
- 完整可运行 Demo

**这是展示效果最直观的一部分，非常关键。**

---

# 👤 **成员 E：数据与运营工具负责人（Dataset & Infra Engineer）**

### **职责**

1. 负责 meme 数据集收集、整理、分类
2. 文件命名规范、metadata 构建（tag、情绪标签）
3. 批量 embedding 生成
4. 管理 `meme_images/` 目录
5. 整理 pipeline：多线程 embedding、缓存、预处理

### **可交付物**

- `/data/meme_dataset/`
- `/data/meme_metadata.json`
- 数据归档脚本（批量压缩/清理）

**相当于数据工程师角色。**

---

# 👤 **成员 F：测试 & 文档负责人（QA & Documentation Engineer）**

### **职责**

1. 完整的测试：
   - 功能测试
   - 工具调用测试
   - 检索性能测试
   - 生成效果测试
   - Agent 行为一致性测试
2. 文档整理：
   - 安装教程
   - API 文档
   - 使用示例
   - 项目结构说明
3. Benchmark：
   - top-k 点击率
   - 用户情绪 → meme 匹配准确性

### **可交付物**

- `/docs/README.md`
- `/docs/API.md`
- 测试报告 `/docs/test_report.md`
- 单元测试：`tests/*.py`

**最后的文档 + 测试将决定项目质量。**

---

# 🗂 **总结：六人最终分工表**

| 角色   | 成员         | 主要负责内容                     |
| ------ | ------------ | -------------------------------- |
| PM     |              | 整体统筹、架构、集成、报告、演示 |
| 成员 A | 搜索负责人   | Embedding + Faiss + search_meme  |
| 成员 B | Agent 负责人 | LLM 推理链路、tool-calling       |
| 成员 C | 生成负责人   | 模板生成 & AIGC 生成             |
| 成员 D | 前端/UI      | Streamlit/Gradio 界面            |
| 成员 E | 数据负责人   | 数据集、数据清洗、metadata 管理  |
| 成员 F | QA/文档      | 测试、文档、性能评估             |
